<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="col-48 line">
      <input id="company_name" type="text" name="company[name]" placeholder="请输入企业名称" class="col-48"/>
    </div>

    <div class="col-48 line">
      <input id="company_phone" type="text" name="company[phone]" placeholder="请输入手机号码" class="col-48"/>
    </div>

    <div class="col-48 line">
      <input id="company_email" type="text" name="company[email]" placeholder="请输入邮箱地址" class="col-48"/>
    </div>

    <div class="col-48">
      <button class="btn-block btn-primary">企业信息提交</button>
    </div>

<% end %>